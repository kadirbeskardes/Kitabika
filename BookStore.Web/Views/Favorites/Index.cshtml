@model IEnumerable<BookStore.Service.DTOs.FavoriteDto>
@{
    ViewData["Title"] = "Favorilerim";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2><i class="bi bi-star-fill text-warning"></i> Favorilerim</h2>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Ana Sayfa</a></li>
                        <li class="breadcrumb-item active">Favorilerim</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>

    <div class="row">
        @if (Model.Any())
        {
            @foreach (var item in Model)
            {
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card h-100 shadow-sm">
                        <div class="position-relative">
                            <div class="card-img-top-container" style="height: 300px; overflow: hidden;">
                                <img src="@item.Book?.CoverImageUrl" 
                                     alt="@item.Book?.Title" 
                                     class="card-img-top img-fluid" 
                                     style="height: 100%; width: 100%; object-fit: cover;">
                            </div>
                            <div class="position-absolute top-0 start-0 m-2">
                                <span class="badge bg-warning">
                                    <i class="bi bi-star-fill"></i> Favori
                                </span>
                            </div>
                        </div>
                        
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@item.Book?.Title</h5>
                            <p class="card-text text-muted">@item.Book?.Author</p>
                            <p class="card-text text-muted">@item.Book?.CategoryName</p>
                            
                            <div class="price-section mb-2">
                                <span class="h5 text-primary">₺@item.Book?.Price.ToString("N2")</span>
                            </div>

                            <div class="mb-3">
                                <small class="text-muted">
                                    <i class="bi bi-calendar"></i> Eklenme: @item.AddedDate.ToString("dd.MM.yyyy")
                                </small>
                            </div>

                            <div class="mt-auto">
                                <div class="btn-group w-100" role="group">
                                    <a href="@Url.Action("Details", "Home", new { id = item.BookId })" 
                                       class="btn btn-outline-primary">
                                        <i class="bi bi-eye"></i> İncele
                                    </a>
                                    <button type="button" 
                                            class="btn btn-outline-danger remove-from-favorites"
                                            data-book-id="@item.BookId">
                                        <i class="bi bi-star"></i> Kaldır
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <div class="text-center py-5">
                    <i class="bi bi-star display-1 text-muted"></i>
                    <h3 class="mt-3">Favori listeniz boş</h3>
                    <p class="text-muted">Beğendiğiniz kitapları favorilerinize ekleyerek daha sonra kolayca bulabilirsiniz.</p>
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
                        <i class="bi bi-search"></i> Kitap Keşfet
                    </a>
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Favorilerden kaldır
            $('.remove-from-favorites').click(function() {
                var bookId = $(this).data('book-id');
                var card = $(this).closest('.col-md-6, .col-lg-4');
                
                if (confirm('Bu kitabı favorilerinizden kaldırmak istediğinizden emin misiniz?')) {
                    $.post('@Url.Action("RemoveFromFavorites", "Favorites")', {
                        bookId: bookId
                    })
                    .done(function(response) {
                        card.fadeOut(300, function() {
                            $(this).remove();
                            // Sayfa boşsa reload et
                            if ($('.card:visible').length === 0) {
                                location.reload();
                            }
                        });
                        showNotification('Kitap favorilerinizden kaldırıldı', 'success');
                    })
                    .fail(function() {
                        showNotification('Bir hata oluştu', 'error');
                    });
                }
            });
        });

        function showNotification(message, type) {
            var alertClass = type === 'success' ? 'alert-success' : 'alert-danger';
            var notification = `
                <div class="alert ${alertClass} alert-dismissible fade show position-fixed" 
                     style="top: 20px; right: 20px; z-index: 9999;" role="alert">
                    ${message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            `;
            $('body').append(notification);
            setTimeout(function() {
                $('.alert').fadeOut();
            }, 3000);
        }
    </script>
}

<style>
    .card-img-top-container img {
        transition: transform 0.3s ease;
    }
    
    .card:hover .card-img-top-container img {
        transform: scale(1.05);
    }
</style>